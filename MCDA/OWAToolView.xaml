<UserControl x:Class="MCDA.OWAToolView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="500" Width="300">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ListBox Name="layerListBox" ItemsSource="{Binding Path=Fields}" HorizontalAlignment="Left" VerticalAlignment="Top" DataContext="{Binding}" Grid.Column="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                             <Label Name="fieldLabel"  Content="{Binding Path=FieldName, Mode=OneWay}" Grid.Column="0"/>
                             <Slider Name="weightSlider" Minimum="0" Maximum="100" TickFrequency="1" Grid.Column="1"/>
                            <Label Name="rankLabel"  Content="{Binding Path=LayerName, Mode=OneWay}" Grid.Column="2"/>
                    </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    
</UserControl>
